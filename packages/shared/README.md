# packages/shared

Tipos TypeScript compartidos entre `apps/web` y `apps/mobile`: contratos de la API del backend
y modelos de módulo. Definidos una sola vez para evitar duplicación (ver `docs/DECISIONS.md`, D-0013).
